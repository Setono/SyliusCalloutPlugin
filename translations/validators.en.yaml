setono_sylius_callout:
    callout:
        code:
            regex: The code can only contain letters, numbers, underscores ("_"), and dashes ("-").
            unique: There is an existing callout with this code.
        elements:
            count_min: You must select at least one element.
        priority:
            numeric: Priority must be an integer.
